Prerequisites

NodeJS link to install nodeJS
Visual Studio Code link to install VSCode


How to get started

Clone the repository
Open the project in vs code
Open the terminal in the vs code
Now type npm install in terminal and hit enter(which will install dependency node_modules)
Now type npm start in terminal and hit enter(It will start the server and it will redirect to browser) if it didn’t redirect to browser then go the browser and access localhost:3000
